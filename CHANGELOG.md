# 변경 내역

## 1.2.1

- 라운드가 강제 재시작(`rr`)돼 라운드 종료 이벤트 없이 끝나도 `round_end`를 보냅니다. 이전에는 이때 이전 시드의 차단이 최대 5분 동안 남아 새 라운드의 시드가 그동안 보였습니다.
- Forced round restarts (`rr`) now send `round_end` too, so the previous seed's block ends right away instead of lingering for up to 5 minutes.

## 1.2.0

- 콘솔 명령 `slmaps help`, `cancel`, `report`, `log`, `version`을 추가했습니다.
- 인증 코드를 보내기 전에 형식을 확인합니다. 요청 번호나 따옴표로 감싼 값처럼 틀린 입력은 slmaps로 보내지 않고 콘솔에서 이유를 알려 줍니다.
- 인증 코드를 다음 주기까지 기다리지 않고 바로 보냅니다. 인증이 진행 중일 때 새 코드를 넣으면 이전 코드를 대신하고, 이전 코드는 다시 보내지 않습니다.
- 켜진 직후와 12시간마다 slmaps에 최신 버전과 최소 버전을 묻습니다. 설치된 버전이 최소 버전보다 낮으면 등록, 인증, 보고를 멈추고 업데이트 방법을 알려 줍니다. `check_for_updates`로 새 버전 경고만 끌 수 있습니다.
- 콘솔에 나오는 문구를 짧게 다듬었습니다.

## 1.1.0

- 디스코드 `/server claim`으로 받은 코드로 서버를 인증할 수 있습니다. `config.yml`의 `claim_code`에 넣거나 콘솔에서 `slmaps claim <코드>`를 실행하면 됩니다.
- 콘솔 명령 `slmaps status`를 추가했습니다.
- IP나 포트가 달라 운영진 확인이 필요한 인증은 서버를 켜 둔 채 기다리면 승인되는 대로 등록됩니다.
- 인증이 끝나면 `claim_code`를 저절로 비웁니다.

## 1.0.0

- 첫 배포입니다. 1회용 등록 토큰으로 서버를 등록하고 자격을 `credential.yml`에 저장합니다.
- 라운드 시작과 종료에 현재 맵 시드를 알리고, `report_interval_seconds` 간격으로 주기 보고를 보냅니다.
- 라운드 ID, 라운드 시작 시각, 라운드 경과 시간은 설정으로 켤 수 있습니다.
- 실패한 요청은 간격을 늘려 가며 다시 시도하고, 자격이 거부되면 보고를 멈춥니다.
